<h4>注文詳細画面</h4>
<h4>注文情報</h4>
<div>
  <p>注文日<%= @order.created_at %></p>
  <p>配送先<%= @order.address %></p>
  <p>支払方法<%= @order.payment_method %></p>
</div>

<% @orders.each do |order| %>
        <p>注文日<%= order.created_at  %></p>
        <p>配送先<%= order.address %></p>
        <p>注文商品<%= order.name %> </p>
        <p>支払金額<%= %>
        <p>注文詳細<%= %></br>
        <%= link_to "表示する",order_path(order.id) %>
  <% end %>

<h4>請求情報</h4>
<div>
  <p>商品合計<%= @total %></p>
  <p>配送料<%= @shipping_cost %></p>
  <p>ご請求額<%= @total_payment %></p>
</div>