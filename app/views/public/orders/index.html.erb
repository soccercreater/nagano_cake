 <table class="table">
  <thead>
   <h2>注文履歴一覧</h2>
     <th scope="col">注文日</th>
     <th>配送先</th>
     <th>注文商品</th>
     <th>支払金額</th>
     <th>注文詳細</th>
      <tr>
       <tr><% @orders.each do |order| %>
        <th scope="col"><%= order.created_at %></th>
        <th scope="col"><%= order.address %></th>
        <th scope="col"><%= order.name %></th>
        <th scope="col"><%= order.payment_method %></th>
        <th scope="col"><%= link_to "表示する",order_path(order.id) %></th>
       </tr>
        <% end %>
   </thead>
  <tbody>
  </tbody>
 </table>