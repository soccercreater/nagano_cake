 <table class="table">
  <thead>
   <h2>注文履歴一覧</h2>
   <th scope="col">購入日時</th>
     <th>購入者</th>
     <th>注文個数</th>
    <% @orders.each do |order| %>
      <tr>
       <th scope="col"><%= order.created_at %></th>
       <th scope="col"><%= order.customer_id %></th>
       <th scope="col"><%#= @customer.first_name %></th>
      <% end %>
      </tr>
   </thead>
  <tbody>
  </tbody>
 </table>
