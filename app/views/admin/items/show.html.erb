<div>
  <p>商品画像：<%= @item.image_id %></p>
  <%= image_tag @item.image, size: "300x300" %>
  <p>商品名：<%= @item.name %></p>
  <p>商品説明：<%= @item.introduction %>
  <p>ジャンル：<%= @item.genre_id %></p>
  <p>税込価格：<%= @item.price %></p>
  <p>販売ステータス：<%= @item.is_active %></p>
</div>
<%= link_to "編集する",edit_admin_item_path %>

